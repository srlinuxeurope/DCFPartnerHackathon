# Generic test users for the DCF lab RADIUS server.
# The SR OS BNG/subscriber (Alc-*) entries were removed together with the
# SR OS PEs; add NAS-specific attributes here if a new consumer is attached.

testuser                                Cleartext-Password := "dcf-hackathon"
                                        Reply-Message = "Hello from the DCF lab RADIUS"

DEFAULT                                 Auth-Type := Accept
                                        Reply-Message = "Accepted by default policy"
